import Link from "next/link";
import tools from "@/data/tools";

const featuredTools = Object.entries(tools).slice(0, 24);

export default function Home() {
  return (
    <main className="min-h-screen bg-slate-950 text-white">
      <section className="mx-auto max-w-7xl px-6 py-16">
        <div className="max-w-3xl">
          <p className="mb-3 text-sm font-semibold uppercase tracking-wider text-cyan-400">
            Free Online SEO Tools
          </p>

          <h1 className="text-4xl font-bold tracking-tight sm:text-6xl">
            Simple SEO tools that help you create, analyze, and optimize content.
          </h1>

          <p className="mt-6 max-w-2xl text-lg leading-8 text-slate-300">
            Use practical browser-based tools for content, keywords, text,
            metadata, and everyday SEO tasks.
          </p>

          <div className="mt-8 flex flex-wrap gap-4">
            <Link
              href="/tools"
              className="rounded-lg bg-cyan-500 px-5 py-3 font-semibold text-slate-950 hover:bg-cyan-400"
            >
              Explore all tools
            </Link>

            <Link
              href="/blog"
              className="rounded-lg border border-slate-700 px-5 py-3 font-semibold hover:bg-slate-900"
            >
              Read the SEO blog
            </Link>
          </div>
        </div>

        <section className="mt-16">
          <div className="mb-6 flex items-end justify-between gap-4">
            <div>
              <h2 className="text-2xl font-bold">Featured tools</h2>
              <p className="mt-2 text-slate-400">
                Start with one of our most useful tools.
              </p>
            </div>

            <Link
              href="/tools"
              className="text-sm font-semibold text-cyan-400 hover:text-cyan-300"
            >
              View all →
            </Link>
          </div>

          <div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">
            {featuredTools.map(([slug, tool]) => (
              <Link
                key={slug}
                href={`/tools/${slug}`}
                className="rounded-xl border border-slate-800 bg-slate-900 p-5 transition hover:border-cyan-500 hover:bg-slate-800"
              >
                <h3 className="font-semibold">{tool.title}</h3>
                <p className="mt-2 text-sm leading-6 text-slate-400">
                  {tool.description}
                </p>
              </Link>
            ))}
          </div>
        </section>
      </section>
    </main>
  );
}
